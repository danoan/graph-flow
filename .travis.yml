dist: trusty
sudo: false
language: cpp
compiler: gcc

os: linux

services:
  - docker

before_install:
  - docker pull danoan/graphflow:dgtal
  - docker build . -t graphflow:build
  
script:
  - docker run graphflow:build /bin/sh -c /app/scripts/test-graph-flow.sh
